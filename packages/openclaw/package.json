{
  "name": "@band-ai/openclaw-channel-band",
  "version": "0.1.5",
  "description": "OpenClaw channel plugin to connect with the Band AI agent collaboration platform",
  "author": "band.ai",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thenvoi/thenvoi-sdk-typescript.git",
    "directory": "packages/openclaw"
  },
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "openclaw.plugin.json"
  ],
  "scripts": {
    "build": "pnpm --filter @band-ai/sdk build && tsup && node scripts/sync-plugin-version.js",
    "dev": "tsup --watch",
    "typecheck": "pnpm --filter @band-ai/sdk build && tsc --noEmit",
    "lint": "eslint src/",
    "test": "pnpm --filter @band-ai/sdk build && vitest run",
    "test:watch": "vitest",
    "test:coverage": "pnpm --filter @band-ai/sdk build && vitest run --coverage",
    "test:e2e": "pnpm --filter @band-ai/sdk build && node --env-file=.env node_modules/vitest/vitest.mjs run --config vitest.config.e2e.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@band-ai/sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^3.2.4",
    "eslint": "^9.0.0",
    "@typescript-eslint/eslint-plugin": "^8.26.0",
    "openclaw": "^2026.4.23",
    "tsup": "^8.0.0",
    "typescript": "^5.8.2",
    "typescript-eslint": "^8.54.0",
    "vitest": "^3.2.4"
  },
  "peerDependencies": {
    "openclaw": ">=2026.1.0"
  },
  "engines": {
    "node": ">=22.14.0"
  },
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ]
  }
}
